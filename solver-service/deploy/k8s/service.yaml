# Deliberately ClusterIP only — no Ingress. Reachable in-cluster at
# http://scheduler-api.<namespace>.svc.cluster.local:8080
apiVersion: v1
kind: Service
metadata:
  name: scheduler-api
spec:
  type: ClusterIP
  selector:
    app: scheduler-api
  ports:
    - port: 8080
      targetPort: 8080
